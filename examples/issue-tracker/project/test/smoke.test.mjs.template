import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
const contract = JSON.parse(await readFile(new URL("../contracts/issues-v1.json", import.meta.url), "utf8"));
assert.equal(contract.version, 1);
assert.ok(contract.create.includes("DIRECT"));
