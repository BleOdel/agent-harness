Report the accepted behavior, test command, observed failing result before the
fix, and passing result after it. Name the collected test file in the claim's
criteria. Add concise command/result notes in workflowEvidence if useful.
A report does not replace an executed assertion, host gates, or review.
